<%= turbo_frame_tag :space do %>
  <%= render "spaces/header", space: @space %>

  <%= link_to t('orders.new'), new_space_order_path(space_id: @space.id), class: "text-center bg-gray-200 hover:bg-gray-300 text-dark font-semibold py-2 px-4 border-transparent rounded" %>
  <br><br>
  <%= render 'orders/search_and_filter', space: @space %>

  <%= render 'orders/table', orders: @orders, space: @space, pagy: @pagy %>
<% end %>
