<div id="user-search">
  <%= form_with url: users_url, method: :get, builder: TailwindBuilder, html: { class: "pb-5 flex flex-wrap gap-2", data: { controller: "autosave" } } do |form| %>
    <div class="relative rounded-md shadow-sm">
      <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
        <svg class="h-5 w-5 text-gray-400" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
        </svg>
      </div>
      <%= search_field_tag :search,
                          params[:search],
                          class: "pl-10 block w-full rounded-md border-gray-300 shadow-sm",
                          placeholder: t('actions.search'),
                          data: { action: 'keyup->autosave#save' } %>
    </div>
    <%= select_tag :admin,
        options_for_select([
          [t('admin', scope: 'activerecord.attributes.user'), nil],
          ['True', true],
          ['False', false]
        ], params[:admin]),
        class: 'rounded-md border-gray-300 shadow-sm',
        data: { action: 'autosave#save' } %>
    <%= select_tag :confirmed,
        options_for_select([
          [t('confirmed', scope: 'activerecord.attributes.user'), nil],
          ['True', true],
          ['False', false]
        ], params[:confirmed]),
        class: 'rounded-md border-gray-300 shadow-sm',
        data: { action: 'autosave#save' } %>
  <% end %>
</div>
