<%= link_to space_product_path(product, space_id: space.id) do %>
  <div class="w-full text-center bg-transparent my-2 py-2 px-5 border rounded hover:bg-blue-500 hover:text-white hover:border-transparent">
    <%= product.description %>
    <br> <%= t('sales_count', scope: 'activerecord.attributes.product' ) %> : <%= product.sales_count %>
  </div>
<% end %>
