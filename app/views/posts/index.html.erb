<%= turbo_frame_tag :content do %>
  <% if can? :create, Post %>
    <%= link_to t('posts.new'), new_post_path, data: { turbo: false }, class: "text-center bg-gray-200 hover:bg-gray-300 text-dark font-semibold py-2 px-4 border-transparent rounded" %>
    <br><br>
  <% end %>
  <% if @posts.count.zero? %>
    <p><%= t('alert.none', class_name: t('post', scope: 'activerecord.models').pluralize.downcase) %></p>
  <% else %>
    <%== render partial: 'shared/pagy/nav', locals: { pagy: @pagy } %>
    <%= render @posts %>
    <%== render partial: 'shared/pagy/nav', locals: { pagy: @pagy } %>
  <% end %>
<% end %>
